<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.style.modules.system.dao.IndexDao">

    <!-- 查询数据
    <select id="findList" resultType="Area">
        SELECT ${sqlMap.column.toSql()}
        FROM ${sqlMap.table.toSql()}
        <where>
            ${sqlMap.where.toSql()}
        </where>
        ORDER BY ${sqlMap.order.toSql()}
    </select> -->

    <insert id="addData">
		insert  into sys_log (id,log_type,log_title,create_by ) VALUES('1','1','1','1');
	</insert>

    <insert id="addTestData">
		insert  into sys_log (id,log_type ) VALUES('1','1');
	</insert>

    <select id="ggg" resultType="java.util.LinkedHashMap">

    </select>

</mapper>